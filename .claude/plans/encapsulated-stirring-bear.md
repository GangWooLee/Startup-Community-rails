# Help Desk 페이지 리디자인 계획

## 요청 요약
레퍼런스 이미지 기반으로 Help Desk (문의하기) 페이지를 완전히 리디자인합니다.

## 현재 상태
- `app/views/inquiries/index.html.erb` - 기본 목록 뷰 (리디자인 필요)
- `app/views/inquiries/new.html.erb` - 기본 폼 뷰 (유지)
- `app/views/inquiries/show.html.erb` - 기본 상세 뷰 (유지)
- `app/models/inquiry.rb` - 모델 존재 (categories: bug/feature/improvement/other)
- `app/helpers/inquiries_helper.rb` - 뱃지 헬퍼 존재
- `app/javascript/controllers/accordion_controller.js` - 아코디언 컨트롤러 존재

## 목표 레이아웃 (레퍼런스 기반)

```
┌─────────────────────────────────────────────────────────────────┐
│ Global Sidebar │ Main Content (Help Desk)                       │
│                │ ┌───────────────────────────────────────────┐  │
│                │ │ Header: 제목 + 필터 탭                    │  │
│                │ ├───────────────────────────────────────────┤  │
│                │ │                                           │  │
│                │ │ ┌─────────────────┐ ┌─────────────────┐   │  │
│                │ │ │ Left Column     │ │ Right Column    │   │  │
│                │ │ │ (8 cols)        │ │ (4 cols)        │   │  │
│                │ │ │                 │ │                 │   │  │
│                │ │ │ FAQ Accordion   │ │ Operating Hours │   │  │
│                │ │ │                 │ │ (Dark Widget)   │   │  │
│                │ │ │ ─────────────── │ │                 │   │  │
│                │ │ │                 │ │                 │   │  │
│                │ │ │ 1:1 Inquiry     │ │                 │   │  │
│                │ │ │ Card List       │ │                 │   │  │
│                │ │ │                 │ │                 │   │  │
│                │ │ └─────────────────┘ └─────────────────┘   │  │
│                │ │                                           │  │
│                │ │                         [FAB: 새 문의]    │  │
│                │ └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 구현 계획

### 1. index.html.erb 전체 리디자인

**파일**: `app/views/inquiries/index.html.erb`

#### 1.1 레이아웃 구조
- Global Sidebar 레이아웃 사용 (`data-controller="navigation"`)
- 12컬럼 그리드: 좌측 8 + 우측 4
- Sticky 헤더 + 탭
- 반응형: lg 이상에서 2컬럼, 미만에서 1컬럼

#### 1.2 필터 탭 (전체/대기중/답변완료)
- Pill 스타일 버튼 (`rounded-full`)
- 활성 탭: `bg-stone-900 text-white`
- 비활성 탭: `bg-stone-100 text-stone-600`
- Turbo Frame으로 리스트만 업데이트

#### 1.3 FAQ 아코디언 섹션
- 기존 `accordion_controller.js` 활용
- 4개 FAQ 항목 (하드코딩 → 추후 DB 이동 가능)
- `data-accordion-allow-multiple-value="false"` (한 번에 하나만)

#### 1.4 1:1 문의 카드 리스트
- 카드 partial `_inquiry_card.html.erb` 생성
- 상태별 아이콘/색상 (대기: 주황, 완료: 초록)
- 호버 시 오렌지 강조

#### 1.5 운영시간 위젯 (다크 테마)
- `bg-[#2C2825]` + `text-[#FDFBF7]`
- Decorative glow 효과
- 평일/주말 운영시간 표시

#### 1.6 FAB 버튼
- `fixed bottom-24 right-6` (모바일)
- `md:bottom-8 md:right-8` (데스크톱)
- 오렌지 배경 + 호버 스케일 효과

---

### 2. 컨트롤러 수정

**파일**: `app/controllers/inquiries_controller.rb`

```ruby
def index
  @filter = params[:filter] || 'all'
  @inquiries = current_user.inquiries.order(created_at: :desc)

  case @filter
  when 'pending'
    @inquiries = @inquiries.where(status: [:pending, :in_progress])
  when 'answered'
    @inquiries = @inquiries.where(status: [:resolved, :closed])
  end

  @inquiries = @inquiries.page(params[:page]).per(10)
end
```

---

### 3. FAQ 데이터 추가

**파일**: `app/helpers/inquiries_helper.rb`

```ruby
FAQ_ITEMS = [
  { question: "문의 답변은 얼마나 걸리나요?", answer: "..." },
  { question: "버그 신고는 어떻게 하나요?", answer: "..." },
  { question: "기능 제안은 어떻게 반영되나요?", answer: "..." },
  { question: "계정 관련 문의는 어떻게 하나요?", answer: "..." }
].freeze
```

---

## 수정 파일 목록

| 파일 | 변경 내용 | 우선순위 |
|------|----------|---------|
| `app/views/inquiries/index.html.erb` | **전체 리디자인** | 🔴 |
| `app/views/inquiries/_inquiry_card.html.erb` | **신규 생성** | 🔴 |
| `app/controllers/inquiries_controller.rb` | 필터 로직 추가 | 🟡 |
| `app/helpers/inquiries_helper.rb` | FAQ_ITEMS 상수 추가 | 🟡 |

---

## 디자인 토큰

- **배경**: `bg-[#FDFBF7]` (Organic Modern Cream)
- **다크 카드**: `bg-[#2C2825]` (Charcoal)
- **강조색**: `orange-500`, `orange-600`
- **카드**: `card-depth rounded-2xl border-stone-200/60`
- **탭**: `rounded-full px-4 py-2`
- **FAB**: `rounded-full shadow-lg hover:scale-105`

---

## 구현 순서

1. `inquiries_helper.rb` - FAQ_ITEMS 상수 추가
2. `inquiries_controller.rb` - 필터 로직 추가
3. `_inquiry_card.html.erb` - 카드 partial 생성
4. `index.html.erb` - 전체 리디자인

---

## 체크리스트

- [ ] Helper: FAQ_ITEMS 상수 추가
- [ ] Controller: @filter 파라미터 처리
- [ ] Partial: _inquiry_card.html.erb 생성
- [ ] View: 헤더 + 탭 구현
- [ ] View: FAQ 아코디언 섹션
- [ ] View: 문의 카드 리스트
- [ ] View: 운영시간 위젯 (다크)
- [ ] View: FAB 버튼
- [ ] 반응형 테스트
