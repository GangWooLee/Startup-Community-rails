<nav class="fixed bottom-0 left-0 right-0 bg-background border-t border-border z-50">
  <div class="max-w-screen-xl mx-auto px-4">
    <div class="flex items-center justify-around h-16">
      <% nav_items = [
        { href: root_path, icon: 'home', label: '커뮤니티' },
        { href: freelance_index_path, icon: 'briefcase', label: '외주' },
        { href: my_page_path, icon: 'user', label: '마이페이지' }
      ] %>

      <% nav_items.each do |item| %>
        <% is_active = current_page?(item[:href]) %>
        <%= link_to item[:href], class: "flex flex-col items-center justify-center gap-1 flex-1 h-full transition-colors #{is_active ? 'text-primary' : 'text-muted-foreground hover:text-foreground'}" do %>
          <%= render partial: "shared/icons/#{item[:icon]}", locals: { css_class: "h-5 w-5" } %>
          <span class="text-xs font-medium"><%= item[:label] %></span>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
