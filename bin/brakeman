#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Note: Removed --ensure-latest to prevent CI failures when new version is released
# Update brakeman regularly via Dependabot instead

load Gem.bin_path("brakeman", "brakeman")
