<!DOCTYPE html>
<html lang="ko" data-environment="<%= Rails.env %>">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title><%= content_for(:title) || "Undrew - 창업자 커뮤니티" %></title>
    <meta name="description" content="아이디어·사람·외주가 한 공간에서 연결되는 최초의 창업 커뮤니티">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Undrew">
    <meta name="mobile-web-app-capable" content="yes">

    <%# Open Graph 메타 태그 %>
    <%= content_for?(:og_meta) ? yield(:og_meta) : og_meta_tags %>

    <%# Canonical URL %>
    <%= content_for?(:canonical) ? yield(:canonical) : canonical_tag %>

    <%# Structured Data (JSON-LD) - Site-wide %>
    <% if Rails.env.production? %>
      <%= organization_json_ld %>
      <%= website_json_ld %>
    <% end %>

    <%# Page-specific structured data %>
    <%= yield(:structured_data) if content_for?(:structured_data) %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <%# Favicon %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# DNS Prefetch - 외부 도메인 사전 연결으로 성능 개선 %>
    <link rel="dns-prefetch" href="https://fonts.googleapis.com">
    <link rel="dns-prefetch" href="https://fonts.gstatic.com">
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net">
    <link rel="dns-prefetch" href="https://www.googletagmanager.com">
    <link rel="dns-prefetch" href="https://www.clarity.ms">

    <%# Google Fonts - 비동기 로딩으로 렌더 블로킹 방지 %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Nanum+Gothic+Coding:wght@400;700&display=swap">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Nanum+Gothic+Coding:wght@400;700&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript><link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Nanum+Gothic+Coding:wght@400;700&display=swap" rel="stylesheet"></noscript>

    <%# Devicon - 비동기 로딩으로 렌더 블로킹 방지 %>
    <link rel="preload" as="style" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css">
    <link href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript><link href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css" rel="stylesheet"></noscript>

    <%# ActionText (Trix 에디터) 스타일 %>
    <%= stylesheet_link_tag "actiontext" %>

    <%# Tailwind CSS (빌드된 CSS 사용 - 성능 최적화: 2026-01-18) %>
    <%= stylesheet_link_tag "tailwind", data: { turbo_track: "reload" } %>

    <%# CSS Variables and Base Styles - undrew-design %>
    <style>
      :root {
        /* Typography - Montserrat (CDN과 동일) */
        --font-sans: 'Montserrat', 'Nanum Gothic Coding', sans-serif;
        --font-size: 16px;
        --font-weight-normal: 400;
        --font-weight-medium: 500;

        /* Base Colors - undrew-design 기준 */
        --background: 0 0% 100%;
        --foreground: 0 0% 9%;
        --card: 0 0% 100%;
        --card-foreground: 0 0% 9%;
        --popover: 0 0% 100%;
        --popover-foreground: 0 0% 9%;

        /* Primary - #030213 (거의 검정) */
        --primary: 248 76% 5%;
        --primary-foreground: 0 0% 100%;

        /* Secondary - 밝은 보라빛 회색 */
        --secondary: 265 6% 95%;
        --secondary-foreground: 248 76% 5%;

        /* Muted - #ececf0 */
        --muted: 240 10% 94%;
        --muted-foreground: 240 5% 50%;

        /* Accent - #e9ebef */
        --accent: 220 14% 93%;
        --accent-foreground: 248 76% 5%;

        /* Destructive - #d4183d */
        --destructive: 349 82% 46%;
        --destructive-foreground: 0 0% 100%;

        /* Border & Input */
        --border: 0 0% 0% / 0.1;
        --input: 0 0% 0% / 0;
        --input-background: 240 6% 96%;
        --switch-background: 220 7% 81%;
        --ring: 0 0% 71%;

        /* Radius - 0.625rem (10px) */
        --radius: 0.625rem;

        /* Sidebar */
        --sidebar: 0 0% 98.5%;
        --sidebar-foreground: 0 0% 9%;
        --sidebar-primary: 248 76% 5%;
        --sidebar-primary-foreground: 0 0% 98.5%;
        --sidebar-accent: 0 0% 97%;
        --sidebar-accent-foreground: 0 0% 20.5%;
        --sidebar-border: 0 0% 92.2%;
        --sidebar-ring: 0 0% 70.8%;
      }

      .dark {
        --background: 0 0% 9%;
        --foreground: 0 0% 98.5%;
        --card: 0 0% 9%;
        --card-foreground: 0 0% 98.5%;
        --popover: 0 0% 9%;
        --popover-foreground: 0 0% 98.5%;
        --primary: 0 0% 98.5%;
        --primary-foreground: 0 0% 20.5%;
        --secondary: 0 0% 26.9%;
        --secondary-foreground: 0 0% 98.5%;
        --muted: 0 0% 26.9%;
        --muted-foreground: 0 0% 70.8%;
        --accent: 0 0% 26.9%;
        --accent-foreground: 0 0% 98.5%;
        --destructive: 0 62.8% 30.6%;
        --destructive-foreground: 0 0% 98.5%;
        --border: 0 0% 26.9%;
        --input: 0 0% 26.9%;
        --input-background: 0 0% 12%;
        --switch-background: 0 0% 25%;
        --ring: 0 0% 43.9%;
        --sidebar: 0 0% 12%;
        --sidebar-foreground: 0 0% 98.5%;
        --sidebar-primary: 262 83% 58%;
        --sidebar-primary-foreground: 0 0% 98.5%;
        --sidebar-accent: 0 0% 26.9%;
        --sidebar-accent-foreground: 0 0% 98.5%;
        --sidebar-border: 0 0% 26.9%;
        --sidebar-ring: 0 0% 43.9%;
      }

      * {
        border-color: hsl(var(--border));
      }

      body {
        font-family: var(--font-sans);
        background-color: hsl(var(--background));
        color: hsl(var(--foreground));
      }

      /* Devicon 색상 오버라이드 - 브랜드 색상 정확도 향상 */
      .devicon-python-plain.colored {
        color: #3776AB !important; /* Python 공식 파란색 */
      }

      /* ==========================================================================
         Landing Page Animations
         ==========================================================================
         ⚠️ IMPORTANT: DO NOT REMOVE OR MOVE THIS SECTION!

         WHY IS THIS HERE?
         - This project uses built Tailwind CSS (stylesheet_link_tag "tailwind")
         - CSS Variables are defined here for runtime access
         - Animations are kept inline to ensure they work regardless of build state

         UPDATED: 2026-01-18 - CDN → Built CSS 전환
         - 성능 개선: 407KB → 223KB (safelist 포함)
         - 렌더 블로킹 제거 (CDN JavaScript → CSS 직접 로드)
         - 롤백: scripts/rollback-to-cdn.sh 사용

         WHAT'S DEFINED HERE:
         - CSS Variables: colors, typography, border-radius
         - Entry animations: fade-slide-up, fade-slide-right, scale-in
         - Scroll animations: animate-on-scroll, animate-on-scroll-card
         - Animation delays: animate-delay-100 through animate-delay-500
         - Accessibility: prefers-reduced-motion support

         RELATED FILES:
         - app/assets/tailwind/application.css (Tailwind source)
         - app/assets/builds/tailwind.css (빌드 결과물)
         - config/shadcn.tailwind.js (safelist 포함)
         - app/javascript/controllers/scroll_animation_controller.js
         - app/views/onboarding/landing.html.erb (main usage)
         ========================================================================== */

      /* Entry Animations - Hero Section */
      @keyframes fade-slide-up {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }
      @keyframes fade-slide-right {
        from { opacity: 0; transform: translateX(20px); }
        to { opacity: 1; transform: translateX(0); }
      }
      @keyframes scale-in {
        from { opacity: 0; transform: scale(0.98); }
        to { opacity: 1; transform: scale(1); }
      }
      @keyframes scroll-fade-up-30 {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
      }

      /* Animation Classes */
      .animate-fade-slide-up { animation: fade-slide-up 0.6s ease-out forwards; }
      .animate-fade-slide-right { animation: fade-slide-right 0.6s ease-out forwards; }
      .animate-scale-in { animation: scale-in 0.6s ease-out forwards; }

      /* Animation Delays */
      .animate-delay-100 { animation-delay: 0.1s; opacity: 0; }
      .animate-delay-200 { animation-delay: 0.2s; opacity: 0; }
      .animate-delay-300 { animation-delay: 0.3s; opacity: 0; }
      .animate-delay-400 { animation-delay: 0.4s; opacity: 0; }
      .animate-delay-500 { animation-delay: 0.5s; opacity: 0; }

      /* Scroll-Triggered Animations */
      .animate-on-scroll { opacity: 0; transform: translateY(30px); }
      .animate-on-scroll.is-visible { animation: scroll-fade-up-30 0.6s ease-out forwards; }
      .animate-on-scroll-card { opacity: 0; transform: translateY(20px); }
      .animate-on-scroll-card.is-visible { animation: fade-slide-up 0.5s ease-out forwards; }

      /* Accessibility - Reduced Motion */
      @media (prefers-reduced-motion: reduce) {
        .animate-fade-slide-up, .animate-fade-slide-right, .animate-scale-in,
        .animate-on-scroll, .animate-on-scroll-card {
          animation: none !important;
          opacity: 1 !important;
          transform: none !important;
        }
        .animate-delay-100, .animate-delay-200, .animate-delay-300,
        .animate-delay-400, .animate-delay-500 { opacity: 1 !important; }
      }

      /* Chat Header Avatar - 모바일 40px, PC 48px */
      .chat-header-avatar > div {
        width: 40px !important;
        height: 40px !important;
      }
      @media (min-width: 768px) {
        .chat-header-avatar > div {
          width: 48px !important;
          height: 48px !important;
        }
      }

      /* Floating Animation - 로그인/회원가입 캐릭터 */
      @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-8px); }
      }
      .animate-float {
        animation: float 3s ease-in-out infinite;
      }
      @media (prefers-reduced-motion: reduce) {
        .animate-float { animation: none !important; }
      }

      /* Pentagon Radar Chart Animation */
      @keyframes pentagon-scale {
        from {
          opacity: 0;
          transform: scale(0);
          transform-origin: center;
        }
        to {
          opacity: 1;
          transform: scale(1);
          transform-origin: center;
        }
      }
      .animate-pentagon-scale {
        animation: pentagon-scale 0.8s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
      }
      @media (prefers-reduced-motion: reduce) {
        .animate-pentagon-scale { animation: none !important; opacity: 1 !important; }
      }

      /* Creator's Canvas Modal Animations */
      @keyframes canvas-emerge {
        from {
          opacity: 0;
          transform: scale(0.95) translateY(10px);
        }
        to {
          opacity: 1;
          transform: scale(1) translateY(0);
        }
      }
      .animate-canvas-emerge {
        animation: canvas-emerge 0.3s cubic-bezier(0.4, 0, 0.2, 1) forwards;
      }
      @media (prefers-reduced-motion: reduce) {
        .animate-canvas-emerge {
          animation: none !important;
          opacity: 1 !important;
          transform: none !important;
        }
      }
    </style>

    <%# Google Analytics 4 (사용자 행동 분석) %>
    <%
      ga_id = Rails.application.credentials.dig(:google, :analytics_id)
      ga_id ||= ENV['GOOGLE_ANALYTICS_ID']  # Docker 환경 폴백
    %>
    <% if ga_id.present? && Rails.env.production? %>
      <!-- GA4 Script -->
      <script async src="https://www.googletagmanager.com/gtag/js?id=<%= ga_id %>"></script>
      <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', '<%= ga_id %>');

        // Rails Turbo 대응 (SPA 페이지 이동 추적)
        document.addEventListener("turbo:load", function(event) {
          gtag('config', '<%= ga_id %>', {
            'page_location': window.location.href,
            'page_title': document.title
          });
        });
      </script>
    <% end %>

    <%# GA4 맞춤 이벤트 처리 (Flash 기반) %>
    <% if flash[:ga4_event].present? && ga_id.present? %>
      <script>
        document.addEventListener('DOMContentLoaded', function() {
          if (typeof gtag === 'function') {
            gtag('event', '<%= j flash[:ga4_event][:name] %>', <%= (flash[:ga4_event][:params] || {}).to_json %>);
          }
        });
      </script>
    <% end %>
    <% if flash[:ga4_events].present? && ga_id.present? %>
      <script>
        document.addEventListener('DOMContentLoaded', function() {
          if (typeof gtag === 'function') {
            <% flash[:ga4_events].each do |event| %>
              gtag('event', '<%= j event[:name] %>', <%= (event[:params] || {}).to_json %>);
            <% end %>
          }
        });
      </script>
    <% end %>

    <%# GA4 Turbo Stream 이벤트 컨테이너 %>
    <div id="ga4-events" style="display: none;"></div>

    <%# Microsoft Clarity (사용자 행동 분석 - 히트맵, 세션 녹화) %>
    <% if Rails.env.production? %>
      <%= render 'layouts/clarity' %>
    <% end %>

    <%# 카카오 JavaScript SDK (공유 기능용) %>
    <%
      # 카카오 JavaScript 키 (credentials에서 가져옴)
      kakao_js_key = Rails.application.credentials.dig(:kakao, :javascript_key)
    %>
    <% if kakao_js_key.present? %>
      <script src="https://t1.kakaocdn.net/kakao_js_sdk/2.7.9/kakao.min.js"
              crossorigin="anonymous"></script>
      <script>
        // 카카오 SDK 초기화
        document.addEventListener('DOMContentLoaded', function() {
          if (typeof Kakao !== 'undefined' && !Kakao.isInitialized()) {
            Kakao.init('<%= kakao_js_key %>');
            <% if Rails.env.development? %>
            console.log('Kakao SDK initialized:', Kakao.isInitialized());
            <% end %>
          }
        });
      </script>
    <% end %>

    <%= javascript_importmap_tags %>
    <%= Sentry.get_trace_propagation_meta.html_safe if defined?(Sentry) && Sentry.initialized? %>
  </head>

  <body class="font-sans antialiased" data-controller="search-modal">
    <!-- Flash Messages -->
    <%# 로그인 페이지에서는 전역 팝업 표시하지 않음 (인라인 메시지 사용) %>
    <% if flash.any? && controller_name != 'sessions' %>
      <div class="fixed top-0 left-0 right-0 z-[9999] flex justify-center p-4 pointer-events-none">
        <div class="w-full max-w-md space-y-2 pointer-events-auto">
          <% flash.each do |type, message| %>
            <%# GA4 이벤트는 스크립트에서만 사용하므로 팝업으로 표시하지 않음 %>
            <% next if [:ga4_event, :ga4_events].include?(type.to_sym) %>
            <%
              alert_class = case type.to_sym
              when :notice
                "bg-primary text-primary-foreground"
              when :alert
                "bg-destructive text-destructive-foreground"
              when :success
                "bg-green-600 text-white"
              else
                "bg-card text-foreground border border-border"
              end
            %>
            <div class="<%= alert_class %> rounded-lg shadow-lg p-4 flex items-start gap-3 animate-slide-down">
              <% if type.to_sym == :notice || type.to_sym == :success %>
                <svg class="h-5 w-5 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
              <% elsif type.to_sym == :alert %>
                <svg class="h-5 w-5 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              <% else %>
                <svg class="h-5 w-5 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              <% end %>
              <div class="flex-1">
                <p class="text-sm font-medium"><%= message %></p>
              </div>
              <button onclick="this.parentElement.remove()" class="flex-shrink-0 hover:opacity-70 transition-opacity">
                <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
              </button>
            </div>
          <% end %>
        </div>
      </div>

      <style>
        @keyframes slide-down {
          from {
            opacity: 0;
            transform: translateY(-1rem);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
        .animate-slide-down {
          animation: slide-down 0.3s ease-out;
        }
      </style>

      <script>
        // Auto-hide flash messages after 5 seconds
        setTimeout(() => {
          const flashMessages = document.querySelectorAll('.animate-slide-down');
          flashMessages.forEach(msg => {
            msg.style.transition = 'opacity 0.3s, transform 0.3s';
            msg.style.opacity = '0';
            msg.style.transform = 'translateY(-1rem)';
            setTimeout(() => msg.remove(), 300);
          });
        }, 5000);
      </script>
    <% end %>

    <% if show_global_sidebar? %>
      <%#
        Global Sidebar Layout - SaaS Dashboard Style
        Reference: Linear, Raycast, Framer, Untitled UI

        Structure:
        ┌────────────────────────────────────────────┐
        │ Global     │  Compact Header (검색+알림)   │
        │ Sidebar    ├───────────────────────────────┤
        │ (w-64)     │                               │
        │            │  Main Content                 │
        │ - Logo     │  (Fluid Width, Scrollable)    │
        │ - Nav      │                               │
        │ - Boards   │                               │
        │            │                               │
        │ ───────    │                               │
        │ User       │                               │
        │ Widget     │                               │
        └────────────┴───────────────────────────────┘
      %>
      <div class="flex h-screen w-full bg-[#FDFBF7] overflow-hidden"
           data-controller="sidebar-collapse">
        <%# Left: Global Sidebar (hidden on mobile, shown via JS on mobile menu toggle) %>
        <%= render "shared/global_sidebar" %>

        <%# Right: Main Content Area %>
        <div class="flex-1 flex flex-col min-w-0 overflow-hidden">
          <%# Compact Header %>
          <%= render "shared/compact_header" %>

          <%# Scrollable Content %>
          <main class="flex-1 overflow-y-auto sidebar-content-scroll">
            <%= yield %>
          </main>
        </div>
      </div>

      <style>
        /* Sidebar scroll customization */
        .sidebar-scroll::-webkit-scrollbar,
        .sidebar-content-scroll::-webkit-scrollbar {
          width: 4px;
        }
        .sidebar-scroll::-webkit-scrollbar-track,
        .sidebar-content-scroll::-webkit-scrollbar-track {
          background: transparent;
        }
        .sidebar-scroll::-webkit-scrollbar-thumb {
          background: rgba(0, 0, 0, 0.1);
          border-radius: 4px;
        }
        .sidebar-scroll::-webkit-scrollbar-thumb:hover {
          background: rgba(0, 0, 0, 0.2);
        }
        .sidebar-content-scroll::-webkit-scrollbar-thumb {
          background: rgba(0, 0, 0, 0.08);
          border-radius: 4px;
        }
        .sidebar-content-scroll::-webkit-scrollbar-thumb:hover {
          background: rgba(0, 0, 0, 0.15);
        }
      </style>
    <% else %>
      <%# Standard Layout (Login, Landing, Admin, etc.) %>
      <%= yield %>
    <% end %>

    <%# 토스트 알림 컨테이너 %>
    <div id="toast-container"></div>

    <%# 프로필 오버레이 컨테이너 %>
    <div id="profile-overlay-container"></div>

    <%# Command Palette 검색 모달 (Phase 16) %>
    <%= render "shared/search_modal" %>

    <%# 플로팅 글쓰기 버튼 (로그인 시에만) %>
    <%= render "shared/floating_write_button" %>

    <%# Creator's Canvas Modal (글쓰기 모달) %>
    <% if logged_in? %>
      <%= render "posts/canvas_modal" %>
    <% end %>
  </body>
</html>
