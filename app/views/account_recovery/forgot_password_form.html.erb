<div class="min-h-screen bg-background flex items-center justify-center px-4">
  <div class="w-full max-w-md">
    <!-- Logo/Title -->
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-foreground mb-2">스타트업 커뮤니티</h1>
      <p class="text-muted-foreground">아이디어, 사람, 외주가 만나는 곳</p>
    </div>

    <!-- Forgot Password Card -->
    <div class="rounded-xl border bg-card text-card-foreground shadow-lg p-8">
      <h2 class="text-2xl font-bold text-foreground mb-2">비밀번호 찾기</h2>
      <p class="text-muted-foreground mb-6">
        가입 시 등록한 이메일 주소를 입력해주세요.<br>
        비밀번호 재설정 링크를 보내드립니다.
      </p>

      <%= form_with url: forgot_password_path, method: :post, local: true, class: "space-y-4" do |f| %>
        <!-- Email Field -->
        <div class="space-y-2">
          <%= render_label name: "email", label: "이메일" %>
          <%= render_input name: "email",
                          type: :email,
                          id: "email",
                          placeholder: "example@startup.com",
                          required: true,
                          class: "w-full" %>
        </div>

        <!-- Submit Button -->
        <%= render_button "재설정 링크 받기", class: "w-full h-11" %>
      <% end %>

      <!-- Info Note -->
      <div class="mt-4 p-3 bg-muted rounded-lg">
        <p class="text-xs text-muted-foreground">
          <strong>소셜 로그인</strong>으로 가입하셨다면, 해당 소셜 서비스에서 비밀번호를 관리해주세요.
        </p>
      </div>

      <!-- Back to Login -->
      <div class="mt-6 text-center text-sm text-muted-foreground">
        <%= link_to "← 로그인으로 돌아가기", login_path, class: "text-primary font-medium hover:underline" %>
      </div>
    </div>

    <!-- Other Options -->
    <div class="mt-4 text-center text-sm">
      <%= link_to "회원가입", signup_path, class: "text-muted-foreground link-hover relative" %>
    </div>
  </div>
</div>
