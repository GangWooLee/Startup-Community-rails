<%#
  Right Sidebar - Organic Modern 스타일 (Global Sidebar Version)
  Reference: design-sample/community_page/community_page (lines 310-375)

  Features:
  - Notice card (dark theme)
  - Top Makers widget (real ranking data)
  - Footer links

  Uses: CommunityHelper (top_makers, rank_badge_color, rank_ring_color, format_engagement_score)

  Grid: lg:col-span-4 (8+4=12 with Center Content)
%>

<aside class="hidden lg:block lg:col-span-4 sticky top-6 space-y-6">
  <%# Notice Card %>
  <div class="bg-[#2C2825] rounded-2xl p-6 text-[#FDFBF7] shadow-xl relative overflow-hidden group border border-stone-800">
    <%# Decorative glow %>
    <div class="absolute -top-12 -right-12 w-32 h-32 bg-orange-500/20 rounded-full blur-2xl animate-pulse"></div>

    <div class="relative z-10">
      <div class="flex items-center justify-between mb-4">
        <span class="text-[10px] font-bold bg-white/10 px-2 py-1 rounded text-orange-200 border border-white/10">NOTICE</span>
        <svg class="w-4 h-4 text-white/50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5.882V19.24a1.76 1.76 0 01-3.417.592l-2.147-6.15M18 13a3 3 0 100-6M5.436 13.683A4.001 4.001 0 017 6h1.832c4.1 0 7.625-1.234 9.168-3v14c-1.543-1.766-5.067-3-9.168-3H7a3.988 3.988 0 01-1.564-.317z"/>
        </svg>
      </div>
      <h3 class="text-lg font-bold mb-2 tracking-tight">AI 창업 분석</h3>
      <p class="text-stone-300 text-xs mb-5 leading-relaxed font-light">
        당신의 아이디어를 AI로 분석해보세요. 무료로 시작할 수 있습니다!
      </p>
      <%= link_to onboarding_ai_input_path, class: "block w-full bg-white text-[#2C2825] py-2.5 rounded-full text-xs font-bold hover:bg-stone-100 transition-colors text-center" do %>
        시작하기
      <% end %>
    </div>
  </div>

  <%# Top Makers Widget %>
  <div class="card-depth rounded-2xl p-6 border border-stone-200/60">
    <div class="flex justify-between items-center mb-5">
      <h3 class="font-bold text-stone-900 flex items-center gap-2 text-sm">
        <span>🏆 이달의 메이커</span>
      </h3>
      <%# Future: link to full leaderboard %>
      <span class="text-[10px] text-stone-400">최근 30일</span>
    </div>

    <div class="space-y-3">
      <% makers = top_makers(limit: 3) %>
      <% if makers.any? %>
        <% makers.each_with_index do |maker, index| %>
          <% rank = index + 1 %>
          <%= link_to profile_path(maker), class: "flex items-center gap-3 p-2 rounded-xl hover:bg-stone-50 transition-colors cursor-pointer group" do %>
            <div class="relative">
              <%# Avatar %>
              <% if maker.avatar.attached? %>
                <%= image_tag maker.avatar, alt: maker.name, class: "w-10 h-10 rounded-full object-cover ring-2 #{rank_ring_color(rank)}" %>
              <% elsif maker.avatar_url.present? %>
                <%= image_tag maker.avatar_url, alt: maker.name, class: "w-10 h-10 rounded-full object-cover ring-2 #{rank_ring_color(rank)}" %>
              <% else %>
                <div class="w-10 h-10 rounded-full ring-2 <%= rank_ring_color(rank) %> bg-stone-200 flex items-center justify-center text-stone-600 font-bold text-sm">
                  <%= maker.name[0].upcase %>
                </div>
              <% end %>
              <%# Rank Badge %>
              <div class="absolute -top-1 -right-1 w-4 h-4 <%= rank_badge_color(rank) %> text-white rounded-full flex items-center justify-center text-[9px] font-bold ring-2 ring-white">
                <%= rank %>
              </div>
            </div>

            <div class="flex-1 min-w-0">
              <p class="text-sm font-bold text-stone-900 truncate group-hover:text-orange-600 transition-colors">
                <%= maker.name %>
              </p>
              <p class="text-[10px] text-stone-400 truncate">
                <%= maker.bio.present? ? truncate(maker.bio, length: 20) : "Undrew 멤버" %>
              </p>
            </div>

            <span class="text-xs font-bold text-stone-500">
              <%= format_engagement_score(maker.engagement_score.to_i) %>
            </span>
          <% end %>
        <% end %>
      <% else %>
        <%# Empty State %>
        <div class="text-center py-4">
          <p class="text-stone-400 text-sm">아직 활동 데이터가 없습니다</p>
        </div>
      <% end %>
    </div>
  </div>

  <%# Popular Tags (지금 뜨는 주제) %>
  <% tags = popular_tags(limit: 5, days: 7) %>
  <% if tags.any? %>
    <div class="card-depth rounded-2xl p-5 border border-stone-200/60">
      <%# Header %>
      <h3 class="flex items-center gap-2 text-sm font-bold text-stone-800 mb-4">
        <span>🔥</span>
        <span>주간 베스트 토픽</span>
      </h3>

      <%# Tag List %>
      <div class="space-y-1">
        <% tags.each do |tag| %>
          <%= link_to search_path(q: tag[:name]),
                      class: "flex items-center gap-3 p-2 -mx-2 rounded-lg hover:bg-stone-50 transition-colors group" do %>
            <%# Rank (Top 3 in orange) %>
            <span class="w-5 text-center text-sm font-bold <%= tag[:rank] <= 3 ? 'text-orange-500' : 'text-stone-400' %>">
              <%= tag[:rank] %>
            </span>

            <%# Tag Name %>
            <span class="flex-1 text-sm font-medium text-stone-700 group-hover:text-stone-900 transition-colors">
              #<%= tag[:name] %>
            </span>

            <%# Post Count Badge %>
            <span class="text-[10px] text-stone-400 bg-stone-100 px-2 py-0.5 rounded-full">
              <%= tag[:count] %>
            </span>
          <% end %>
        <% end %>
      </div>
    </div>
  <% end %>

  <%# Footer Links %>
  <div class="px-2 pt-2 text-[10px] text-stone-400 flex flex-wrap gap-x-4 gap-y-2">
    <a class="link-glow" href="#">About</a>
    <a class="link-glow" href="#">Privacy</a>
    <a class="link-glow" href="#">Terms</a>
    <a class="link-glow" href="#">Help</a>
    <span>© <%= Date.current.year %> Undrew Inc.</span>
  </div>
</aside>
