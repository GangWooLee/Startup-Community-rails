<%#
  Weekly Best Hero Card - Organic Modern 스타일
  Reference: design-sample/community_page/community_page (lines 174-204)

  Features:
  - Full-width hero image with gradient overlay
  - "Weekly Best 🔥" badge
  - Author avatar, username, time
  - Title and description
  - Hover scale effect on image

  Local variable:
  - post: The weekly best Post object (required)
%>

<% if local_assigns[:post] && post.present? %>
  <%= link_to post_path(post), class: "block relative w-full h-[clamp(250px,40vh,320px)] rounded-2xl overflow-hidden group hover-scale shadow-lg shadow-orange-900/10 hover:shadow-xl hover:shadow-orange-900/15" do %>
    <%# Background Image %>
    <% if post.images.attached? && post.images.first.present? %>
      <%= image_tag post.images.first,
          alt: post.title,
          class: "absolute inset-0 w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-700 ease-out" %>
    <% else %>
      <%# Fallback gradient background %>
      <div class="absolute inset-0 w-full h-full bg-gradient-to-br from-stone-700 via-stone-800 to-stone-900"></div>
    <% end %>

    <%# Gradient Overlay %>
    <div class="absolute inset-0 bg-gradient-to-t from-stone-900/90 via-stone-900/40 to-transparent"></div>

    <%# Content %>
    <div class="absolute inset-0 p-8 flex flex-col justify-end">
      <%# Badge %>
      <div class="absolute top-6 left-6">
        <span class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-bold bg-white/20 text-white backdrop-blur-md border border-white/20">
          Weekly Best 🔥
        </span>
      </div>

      <div class="max-w-2xl">
        <%# Author Info %>
        <div class="flex items-center gap-2 mb-3 text-white/90">
          <% if post.user.avatar.attached? %>
            <%= image_tag post.user.avatar, alt: post.user.display_name, class: "w-6 h-6 rounded-full border border-white/20 object-cover" %>
          <% elsif post.user.avatar_url.present? %>
            <%= image_tag post.user.avatar_url, alt: post.user.display_name, class: "w-6 h-6 rounded-full border border-white/20 object-cover" %>
          <% else %>
            <div class="w-6 h-6 rounded-full border border-white/20 bg-white/20 flex items-center justify-center text-white text-xs font-bold">
              <%= post.user.display_name[0].upcase %>
            </div>
          <% end %>
          <span class="text-sm font-medium">@<%= post.user.display_name %></span>
          <span class="text-xs text-white/60">•</span>
          <span class="text-xs text-white/80"><%= time_ago_in_words(post.created_at) %> 전</span>
        </div>

        <%# Title %>
        <h2 class="text-2xl sm:text-3xl font-bold text-white mb-2 leading-tight tracking-tight line-clamp-2">
          <%= post.title %>
        </h2>

        <%# Description %>
        <% if post.content.present? %>
          <p class="text-stone-200 text-sm md:text-base line-clamp-2 mb-0 font-light opacity-90">
            <%= truncate(strip_tags(post.content), length: 120) %>
          </p>
        <% end %>
      </div>
    </div>
  <% end %>
<% else %>
  <%# Empty State - No Weekly Best %>
  <div class="relative w-full h-[clamp(250px,40vh,320px)] rounded-2xl overflow-hidden bg-gradient-to-br from-stone-200 via-stone-100 to-white border border-stone-200/60 shadow-sm flex items-center justify-center">
    <div class="text-center px-8">
      <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-stone-100 flex items-center justify-center">
        <svg class="w-8 h-8 text-stone-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
        </svg>
      </div>
      <p class="text-stone-500 font-medium">이번 주 인기 글이 아직 없습니다</p>
      <p class="text-stone-400 text-sm mt-1">첫 번째 인기 글의 주인공이 되어보세요!</p>
    </div>
  </div>
<% end %>
