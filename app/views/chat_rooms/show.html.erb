<div class="flex flex-col h-[calc(100vh-64px)] bg-background"
     data-controller="chat-room"
     data-chat-room-room-id-value="<%= @chat_room.id %>">

  <!-- 헤더: 프로필 강화 -->
  <header class="flex items-center gap-3 px-4 py-3 border-b border-gray-200 bg-white sticky top-0 z-20 shadow-sm">
    <%= link_to chat_rooms_path, class: "text-gray-600 hover:text-gray-900 transition-colors flex-shrink-0 -ml-1 p-1" do %>
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
    <% end %>

    <%= link_to profile_path(@other_user), class: "flex items-center gap-3 flex-1 min-w-0 group" do %>
      <!-- 프로필 이미지 (이니셜 폴백 포함) -->
      <div class="relative flex-shrink-0">
        <% if @other_user.avatar&.attached? %>
          <%= image_tag @other_user.avatar, class: "w-10 h-10 rounded-full object-cover border border-gray-200" %>
        <% else %>
          <div class="w-10 h-10 rounded-full flex items-center justify-center text-white font-semibold <%= avatar_bg_color(@other_user.name) %>">
            <%= @other_user.name&.first&.upcase || "?" %>
          </div>
        <% end %>
        <!-- 온라인 상태 표시 (선택적) -->
        <%# <span class="absolute bottom-0 right-0 w-3 h-3 bg-green-500 border-2 border-white rounded-full"></span> %>
      </div>

      <div class="min-w-0 flex-1">
        <div class="flex items-center gap-2">
          <p class="font-semibold text-gray-900 truncate group-hover:text-primary transition-colors"><%= @other_user.name %></p>
          <% if @other_user.provider.present? %>
            <span class="inline-flex items-center gap-0.5 px-1.5 py-0.5 rounded text-[10px] font-medium bg-blue-100 text-blue-700">
              <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              인증됨
            </span>
          <% end %>
        </div>
        <p class="text-xs text-gray-500 truncate">
          <% if @other_user.role_title.present? %>
            <%= @other_user.role_title %>
            <% if @other_user.affiliation.present? %>
              <span class="text-gray-400">@</span><%= @other_user.affiliation %>
            <% end %>
          <% else %>
            프로필 보기 →
          <% end %>
        </p>
      </div>
    <% end %>

    <!-- 더보기 메뉴 (드롭다운) -->
    <div class="relative" data-controller="dropdown">
      <button class="text-gray-500 hover:text-gray-700 transition-colors flex-shrink-0 p-1 rounded-lg hover:bg-gray-100"
              type="button"
              data-action="click->dropdown#toggle">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 5v.01M12 12v.01M12 19v.01M12 6a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2zm0 7a1 1 0 110-2 1 1 0 010 2z"/>
        </svg>
      </button>

      <div class="absolute right-0 top-full mt-1 w-48 bg-white rounded-xl shadow-lg border border-gray-200 py-1 hidden z-30"
           data-dropdown-target="menu">
        <%= link_to profile_path(@other_user), class: "flex items-center gap-2 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
          </svg>
          프로필 보기
        <% end %>
        <% if @chat_room.source_post.present? %>
          <%= link_to post_path(@chat_room.source_post), class: "flex items-center gap-2 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" do %>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
            게시글 보기
          <% end %>
        <% end %>
        <div class="border-t border-gray-100 my-1"></div>
        <button type="button" class="flex items-center gap-2 px-4 py-2 text-sm text-red-600 hover:bg-red-50 w-full text-left">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
          </svg>
          신고하기
        </button>
      </div>
    </div>
  </header>

  <!-- 게시글 컨텍스트 카드 (있을 경우만 표시) -->
  <% if @chat_room.source_post.present? %>
    <%= render "chat_rooms/source_post_card", post: @chat_room.source_post, chat_room: @chat_room %>
  <% end %>

  <!-- 거래 상태 변경 버튼 (게시글 작성자에게만 표시) -->
  <% if @chat_room.source_post.present? && @chat_room.post_author?(current_user) %>
    <%= render "chat_rooms/deal_actions", chat_room: @chat_room %>
  <% end %>

  <!-- 메시지 영역 -->
  <%= turbo_stream_from @chat_room %>
  <div id="messages"
       class="flex-1 overflow-y-auto p-4 space-y-3 bg-gray-50"
       data-chat-room-target="messages">

    <% if @messages.empty? %>
      <!-- 빈 상태: 첫 대화 시작 안내 -->
      <div class="flex flex-col items-center justify-center h-full py-12">
        <div class="w-20 h-20 rounded-full bg-primary/10 flex items-center justify-center mb-4">
          <svg class="w-10 h-10 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
          </svg>
        </div>
        <p class="font-medium text-gray-800 mb-1"><%= @other_user.name %>님과의 대화</p>
        <p class="text-sm text-gray-500 text-center max-w-xs">
          인사말을 건네고 협업의 기회를 잡아보세요!
        </p>
        <div class="flex flex-wrap justify-center gap-2 mt-4">
          <button type="button" class="suggestion-chip" data-action="click->chat-room#insertSuggestion" data-message="안녕하세요! 반갑습니다 😊">
            안녕하세요! 반갑습니다
          </button>
          <button type="button" class="suggestion-chip" data-action="click->chat-room#insertSuggestion" data-message="게시글 보고 연락드립니다">
            게시글 보고 연락드립니다
          </button>
          <button type="button" class="suggestion-chip" data-action="click->chat-room#insertSuggestion" data-message="협업에 관심이 있어서 연락드렸어요">
            협업에 관심이 있어요
          </button>
        </div>
      </div>
    <% else %>
      <!-- 안전 거래 경고 -->
      <div class="flex justify-center mb-2">
        <div class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-amber-50 border border-amber-200">
          <svg class="w-4 h-4 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
          </svg>
          <p class="text-xs text-amber-700">직거래 시 발생하는 문제에 대해 플랫폼은 책임지지 않습니다</p>
        </div>
      </div>

      <!-- 메시지 목록 -->
      <% @messages.each do |message| %>
        <%= render "messages/message", message: message %>
      <% end %>
    <% end %>
  </div>

  <!-- 퀵 전송 버튼 -->
  <div class="px-4 py-2 border-t border-gray-200 bg-white">
    <div class="flex gap-2 overflow-x-auto scrollbar-hide pb-1">
      <%= button_to send_profile_chat_room_messages_path(@chat_room),
                    method: :post,
                    class: "quick-action-btn",
                    data: { turbo_stream: true } do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
        </svg>
        내 프로필
      <% end %>

      <% if current_user.open_chat_url.present? || current_user.email.present? %>
        <%= button_to send_contact_chat_room_messages_path(@chat_room),
                      method: :post,
                      class: "quick-action-btn",
                      data: { turbo_stream: true } do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
          </svg>
          연락처
        <% end %>
      <% end %>

      <% if @chat_room.source_post.present? && !@chat_room.post_author?(current_user) && @chat_room.pending? %>
        <!-- 지원자가 볼 수 있는 버튼 -->
        <button type="button" class="quick-action-btn text-green-700 border-green-200 hover:bg-green-50">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          지원 의사 전달
        </button>
      <% end %>
    </div>
  </div>

  <!-- 입력 영역 -->
  <div class="p-3 border-t border-gray-200 bg-white safe-area-bottom">
    <%= form_with model: [@chat_room, Message.new],
                  data: {
                    controller: "message-form",
                    action: "turbo:submit-end->message-form#reset",
                    chat_room_target: "form"
                  },
                  class: "flex items-end gap-2" do |f| %>
      <%= f.text_area :content,
                      placeholder: "메시지를 입력하세요...",
                      rows: 1,
                      class: "flex-1 px-4 py-2.5 rounded-2xl border border-gray-300 bg-gray-50 text-gray-900 placeholder-gray-500 resize-none focus:bg-white focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary max-h-32 text-sm",
                      data: {
                        message_form_target: "input",
                        chat_room_target: "messageInput",
                        action: "input->message-form#autoResize keydown.ctrl+enter->message-form#submit keydown.meta+enter->message-form#submit"
                      } %>
      <%= f.submit "전송",
                   class: "px-5 py-2.5 bg-primary text-white rounded-full font-medium text-sm hover:bg-primary/90 transition cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed",
                   data: { message_form_target: "button" } %>
    <% end %>
  </div>
</div>

<style>
  .suggestion-chip {
    @apply px-3 py-1.5 text-xs font-medium text-gray-600 bg-white border border-gray-200 rounded-full hover:bg-gray-50 hover:border-gray-300 transition-colors whitespace-nowrap;
  }

  .quick-action-btn {
    @apply inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-xs font-medium text-gray-700 bg-white border border-gray-200 hover:bg-gray-100 transition whitespace-nowrap;
  }
</style>
