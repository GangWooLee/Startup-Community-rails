<div class="min-h-screen bg-background flex items-center justify-center px-4 py-12">
  <div class="w-full max-w-md">
    <!-- Logo/Title -->
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-foreground mb-2">스타트업 커뮤니티</h1>
      <p class="text-muted-foreground">아이디어, 사람, 외주가 만나는 곳</p>
    </div>

    <!-- Signup Card -->
    <div class="bg-card rounded-xl p-8 border border-border shadow-lg">
      <h2 class="text-2xl font-bold text-foreground mb-6">회원가입</h2>

      <%= form_with model: @user, url: signup_path, method: :post, local: true, class: "space-y-4" do |f| %>
        <!-- Error Messages -->
        <% if @user.errors.any? %>
          <div class="rounded-md bg-destructive/10 border border-destructive/20 p-4">
            <div class="flex items-start gap-3">
              <svg class="h-5 w-5 text-destructive flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <div class="flex-1">
                <h3 class="text-sm font-medium text-destructive mb-2">
                  <%= pluralize(@user.errors.count, "개의") %> 오류가 발생했습니다:
                </h3>
                <ul class="list-disc list-inside text-sm text-destructive space-y-1">
                  <% @user.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        <% end %>

        <!-- Name Field -->
        <div>
          <%= f.label :name, "이름 *", class: "block text-sm font-medium text-foreground mb-2" %>
          <%= f.text_field :name,
              placeholder: "홍길동",
              required: true,
              class: "w-full px-3 py-2 rounded-md border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring" %>
        </div>

        <!-- Email Field -->
        <div>
          <%= f.label :email, "이메일 *", class: "block text-sm font-medium text-foreground mb-2" %>
          <%= f.email_field :email,
              placeholder: "example@startup.com",
              required: true,
              class: "w-full px-3 py-2 rounded-md border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring" %>
        </div>

        <!-- Password Field -->
        <div>
          <%= f.label :password, "비밀번호 * (최소 6자)", class: "block text-sm font-medium text-foreground mb-2" %>
          <%= f.password_field :password,
              placeholder: "••••••••",
              required: true,
              minlength: 6,
              class: "w-full px-3 py-2 rounded-md border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring" %>
        </div>

        <!-- Password Confirmation Field -->
        <div>
          <%= f.label :password_confirmation, "비밀번호 확인 *", class: "block text-sm font-medium text-foreground mb-2" %>
          <%= f.password_field :password_confirmation,
              placeholder: "••••••••",
              required: true,
              minlength: 6,
              class: "w-full px-3 py-2 rounded-md border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring" %>
        </div>

        <!-- Role Title Field (Optional) -->
        <div>
          <%= f.label :role_title, "직함/역할 (선택)", class: "block text-sm font-medium text-foreground mb-2" %>
          <%= f.text_field :role_title,
              placeholder: "예: 개발자, 디자이너, 기획자",
              class: "w-full px-3 py-2 rounded-md border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring" %>
          <p class="mt-1 text-xs text-muted-foreground">프로필에 표시될 직함을 입력하세요</p>
        </div>

        <!-- Bio Field (Optional) -->
        <div>
          <%= f.label :bio, "자기소개 (선택)", class: "block text-sm font-medium text-foreground mb-2" %>
          <%= f.text_area :bio,
              placeholder: "간단한 자기소개를 작성하세요...",
              rows: 3,
              maxlength: 500,
              class: "w-full px-3 py-2 rounded-md border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring resize-none" %>
          <p class="mt-1 text-xs text-muted-foreground">최대 500자</p>
        </div>

        <!-- Terms Agreement -->
        <div class="flex items-start gap-2">
          <input type="checkbox" id="terms" required class="mt-1 rounded border-border">
          <label for="terms" class="text-sm text-muted-foreground">
            <a href="#" class="text-primary hover:underline">이용약관</a> 및
            <a href="#" class="text-primary hover:underline">개인정보 처리방침</a>에 동의합니다 *
          </label>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="w-full inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50 bg-primary text-primary-foreground shadow hover:bg-primary/90 h-11 px-8">
          회원가입
        </button>
      <% end %>

      <!-- Login Link -->
      <div class="mt-6 text-center text-sm text-muted-foreground">
        이미 계정이 있으신가요?
        <%= link_to "로그인", login_path, class: "text-primary font-medium hover:underline ml-1" %>
      </div>
    </div>

    <!-- Back to Home -->
    <div class="mt-4 text-center">
      <%= link_to "← 홈으로 돌아가기", root_path, class: "text-sm text-muted-foreground hover:text-foreground transition-colors" %>
    </div>
  </div>
</div>
