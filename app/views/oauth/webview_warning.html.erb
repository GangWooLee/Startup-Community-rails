<div class="min-h-screen w-full flex items-center justify-center bg-background px-4 py-8 relative overflow-hidden">
  <!-- Subtle background pattern -->
  <div class="absolute inset-0 opacity-[0.02]" style="background-image: radial-gradient(circle at 1px 1px, currentColor 1px, transparent 0); background-size: 32px 32px;"></div>

  <!-- Decorative gradient blurs -->
  <div class="absolute top-0 left-1/4 w-96 h-96 bg-amber-500/5 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-amber-500/5 rounded-full blur-3xl"></div>

  <!-- Main Content -->
  <div class="relative z-10 w-full max-w-md">
    <!-- Warning Icon Header -->
    <div class="text-center mb-6">
      <div class="flex justify-center mb-4">
        <div class="w-24 h-24 rounded-full bg-amber-100 flex items-center justify-center">
          <svg class="w-12 h-12 text-amber-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
          </svg>
        </div>
      </div>

      <!-- Title -->
      <h1 class="text-2xl font-bold text-foreground mb-2">브라우저에서 열어주세요</h1>
      <p class="text-muted-foreground text-sm">
        <span class="font-semibold text-foreground"><%= @detected_app %></span>에서는<br>
        Google 로그인이 지원되지 않습니다
      </p>
    </div>

    <!-- Info Card -->
    <div class="rounded-xl border bg-card text-card-foreground shadow-xl p-6 space-y-5">
      <!-- Explanation -->
      <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 text-sm text-amber-800">
        <p class="font-medium mb-1">왜 이런 건가요?</p>
        <p class="text-amber-700">
          Google은 보안을 위해 앱 내 브라우저에서의 로그인을 제한하고 있습니다.
          Safari나 Chrome 같은 기본 브라우저에서 열어주세요.
        </p>
      </div>

      <!-- Action Buttons -->
      <div class="space-y-3">
        <% if @is_kakao && @is_ios %>
          <%# 카카오톡 iOS: Chrome 우선 + Safari 보조 버튼 %>
          <a
            id="kakao-chrome-btn"
            href="<%= @ios_chrome_url %>"
            class="w-full flex items-center justify-center gap-2 h-12 px-4 py-2 bg-primary text-primary-foreground hover:bg-primary/90 rounded-lg font-semibold transition-colors shadow-sm"
          >
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0C8.21 0 4.831 1.757 2.632 4.501l3.953 6.848A5.454 5.454 0 0 1 12 6.545h10.691A12 12 0 0 0 12 0zM1.931 5.47A11.943 11.943 0 0 0 0 12c0 6.012 4.42 10.991 10.189 11.864l3.953-6.847a5.45 5.45 0 0 1-6.865-2.29zm13.342 2.166a5.446 5.446 0 0 1 1.45 7.09l.002.001h-.002l-5.344 9.257c.206.01.413.016.621.016 6.627 0 12-5.373 12-12 0-1.54-.29-3.011-.818-4.364zM12 16.364a4.364 4.364 0 1 1 0-8.728 4.364 4.364 0 0 1 0 8.728z"/>
            </svg>
            Chrome에서 열기
          </a>

          <a
            id="kakao-safari-btn"
            href="<%= @kakao_external_url %>"
            class="w-full flex items-center justify-center gap-2 h-11 px-4 py-2 border-2 border-border rounded-lg font-medium text-foreground hover:bg-accent transition-colors"
          >
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C6.477 2 2 6.477 2 12s4.477 10 10 10 10-4.477 10-10S17.523 2 12 2zm4.243 14.243l-5.657-2.829-2.829-5.657 5.657 2.829 2.829 5.657zM12 11a1 1 0 100 2 1 1 0 000-2z"/>
            </svg>
            Safari에서 열기
          </a>

          <p class="text-center text-xs text-muted-foreground">
            Chrome이 설치되어 있다면 자동으로 열립니다
          </p>

          <!-- Divider -->
          <div class="relative my-2">
            <div class="absolute inset-0 flex items-center">
              <div class="w-full border-t border-border"></div>
            </div>
            <div class="relative flex justify-center text-xs uppercase">
              <span class="bg-card px-2 text-muted-foreground">열리지 않으면</span>
            </div>
          </div>
        <% elsif @is_kakao %>
          <%# 카카오톡 Android: 기존 외부 브라우저로 열기 버튼 %>
          <a
            id="kakao-external-btn"
            href="<%= @kakao_external_url %>"
            class="w-full flex items-center justify-center gap-2 h-12 px-4 py-2 bg-[#FEE500] hover:bg-[#F5DC00] text-[#3C1E1E] rounded-lg font-semibold transition-colors shadow-sm"
          >
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 3C6.477 3 2 6.477 2 10.5c0 2.47 1.607 4.647 4.035 5.906-.176.648-.638 2.347-.731 2.708-.118.455.167.448.35.326.145-.095 2.305-1.56 3.234-2.19.694.1 1.407.15 2.112.15 5.523 0 10-3.477 10-7.5S17.523 3 12 3z"/>
            </svg>
            외부 브라우저로 열기
          </a>

          <p class="text-center text-xs text-muted-foreground">
            카카오톡에서 자동으로 기본 브라우저가 열립니다
          </p>

          <!-- Divider -->
          <div class="relative my-2">
            <div class="absolute inset-0 flex items-center">
              <div class="w-full border-t border-border"></div>
            </div>
            <div class="relative flex justify-center text-xs uppercase">
              <span class="bg-card px-2 text-muted-foreground">열리지 않으면</span>
            </div>
          </div>
        <% end %>

        <% if @is_android %>
          <%# Android: Intent URI로 Chrome 직접 열기 %>
          <%
            uri = URI.parse(@login_url)
            intent_path = "#{uri.host}#{uri.path}"
            intent_path += "?#{uri.query}" if uri.query.present?
            intent_url = "intent://#{intent_path}#Intent;scheme=https;package=com.android.chrome;S.browser_fallback_url=#{CGI.escape(@login_url)};end;"
          %>
          <a
            href="<%= intent_url %>"
            class="w-full flex items-center justify-center gap-2 h-11 px-4 py-2 bg-primary text-primary-foreground hover:bg-primary/90 rounded-lg font-medium transition-colors"
          >
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0C8.21 0 4.831 1.757 2.632 4.501l3.953 6.848A5.454 5.454 0 0 1 12 6.545h10.691A12 12 0 0 0 12 0zM1.931 5.47A11.943 11.943 0 0 0 0 12c0 6.012 4.42 10.991 10.189 11.864l3.953-6.847a5.45 5.45 0 0 1-6.865-2.29zm13.342 2.166a5.446 5.446 0 0 1 1.45 7.09l.002.001h-.002l-5.344 9.257c.206.01.413.016.621.016 6.627 0 12-5.373 12-12 0-1.54-.29-3.011-.818-4.364zM12 16.364a4.364 4.364 0 1 1 0-8.728 4.364 4.364 0 0 1 0 8.728z"/>
            </svg>
            Chrome에서 열기
          </a>

          <!-- Chrome 없을 때 대비: 링크 복사 -->
          <button
            type="button"
            onclick="copyLoginUrl()"
            class="w-full flex items-center justify-center gap-2 h-11 px-4 py-2 border-2 border-border rounded-lg font-medium text-foreground hover:bg-accent transition-colors"
          >
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
            </svg>
            <span id="copy-button-text">링크 복사하기</span>
          </button>

          <p class="text-center text-xs text-muted-foreground">
            Chrome이 열리지 않으면 링크를 복사해서 붙여넣어 주세요
          </p>
        <% else %>
          <%# iOS: Chrome URL 스킴으로 열기 (googlechromes://) %>
          <%
            uri = URI.parse(@login_url)
            chrome_path = "#{uri.host}#{uri.path}"
            chrome_path += "?#{uri.query}" if uri.query.present?
            ios_chrome_url = "googlechromes://#{chrome_path}"
          %>
          <a
            href="<%= ios_chrome_url %>"
            class="w-full flex items-center justify-center gap-2 h-11 px-4 py-2 bg-primary text-primary-foreground hover:bg-primary/90 rounded-lg font-medium transition-colors"
          >
            <svg class="w-5 h-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0C8.21 0 4.831 1.757 2.632 4.501l3.953 6.848A5.454 5.454 0 0 1 12 6.545h10.691A12 12 0 0 0 12 0zM1.931 5.47A11.943 11.943 0 0 0 0 12c0 6.012 4.42 10.991 10.189 11.864l3.953-6.847a5.45 5.45 0 0 1-6.865-2.29zm13.342 2.166a5.446 5.446 0 0 1 1.45 7.09l.002.001h-.002l-5.344 9.257c.206.01.413.016.621.016 6.627 0 12-5.373 12-12 0-1.54-.29-3.011-.818-4.364zM12 16.364a4.364 4.364 0 1 1 0-8.728 4.364 4.364 0 0 1 0 8.728z"/>
            </svg>
            Chrome에서 열기
          </a>

          <%# Chrome 없을 때 대비: 링크 복사 %>
          <button
            type="button"
            onclick="copyLoginUrl()"
            class="w-full flex items-center justify-center gap-2 h-11 px-4 py-2 border-2 border-border rounded-lg font-medium text-foreground hover:bg-accent transition-colors"
          >
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
            </svg>
            <span id="copy-button-text">링크 복사하기</span>
          </button>

          <p class="text-center text-xs text-muted-foreground">
            Chrome이 열리지 않으면 Safari에서 링크를 붙여넣어 주세요
          </p>
        <% end %>

        <!-- Divider -->
        <div class="relative">
          <div class="absolute inset-0 flex items-center">
            <div class="w-full border-t border-border"></div>
          </div>
          <div class="relative flex justify-center text-xs uppercase">
            <span class="bg-card px-2 text-muted-foreground">또는</span>
          </div>
        </div>

        <!-- GitHub Login (WebView에서 작동) -->
        <%= form_with url: oauth_passthru_path(provider: "github"), method: :post, local: true, data: { turbo: false } do %>
          <%= render_button variant: :outline, class: "w-full h-11 gap-2 border-2 border-border hover:border-foreground/30" do %>
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            GitHub로 로그인하기
          <% end %>
        <% end %>

        <!-- Email Login -->
        <%= link_to login_path, class: "w-full flex items-center justify-center gap-2 h-11 px-4 py-2 border-2 border-border rounded-lg font-medium text-foreground hover:bg-accent transition-colors" do %>
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
          이메일로 로그인하기
        <% end %>
      </div>
    </div>

    <!-- Back to Home -->
    <div class="mt-4 text-center">
      <%= link_to root_path, class: "text-sm text-muted-foreground link-hover relative inline-block" do %>
        ← 처음으로 돌아가기
      <% end %>
    </div>
  </div>
</div>

<script>
// 카카오톡 외부 브라우저 자동 열기 시도
<% if @is_kakao && @is_ios %>
// iOS 카카오톡: Chrome 우선 시도 → visibilitychange로 감지 → Safari 폴백
(function() {
  const chromeUrl = "<%= @ios_chrome_url %>";
  const safariUrl = "<%= @kakao_external_url %>";
  let chromeOpened = false;
  let safariTimeoutId = null;

  // 1. visibilitychange 이벤트 리스너 등록
  //    Chrome이 열리면 페이지가 hidden 상태가 됨
  document.addEventListener('visibilitychange', function() {
    if (document.hidden) {
      chromeOpened = true;
      // Chrome이 열렸으므로 Safari 폴백 취소
      if (safariTimeoutId) {
        clearTimeout(safariTimeoutId);
        safariTimeoutId = null;
      }
    }
  });

  // 2. 500ms 후 Chrome 열기 시도
  setTimeout(function() {
    window.location.href = chromeUrl;
  }, 500);

  // 3. 3.5초 후에도 Chrome이 열리지 않았으면 Safari 폴백
  //    (기존 500ms → 3000ms 갭으로 충분한 여유 확보)
  safariTimeoutId = setTimeout(function() {
    if (!chromeOpened && !document.hidden) {
      window.location.href = safariUrl;
    }
  }, 3500);
})();
<% elsif @is_kakao %>
// Android 카카오톡: 기존 외부 브라우저 열기
(function() {
  const kakaoExternalUrl = "<%= @kakao_external_url %>";

  setTimeout(function() {
    window.location.href = kakaoExternalUrl;
  }, 500);
})();
<% end %>

function copyLoginUrl() {
  const url = "<%= @login_url %>";

  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(url).then(() => {
      showCopySuccess();
    }).catch(() => {
      fallbackCopy(url);
    });
  } else {
    fallbackCopy(url);
  }
}

function fallbackCopy(text) {
  const textarea = document.createElement('textarea');
  textarea.value = text;
  textarea.style.position = 'fixed';
  textarea.style.opacity = '0';
  document.body.appendChild(textarea);
  textarea.select();
  try {
    document.execCommand('copy');
    showCopySuccess();
  } catch (e) {
    alert('링크를 복사할 수 없습니다. 직접 주소를 복사해주세요:\n' + text);
  }
  document.body.removeChild(textarea);
}

function showCopySuccess() {
  const buttonText = document.getElementById('copy-button-text');
  const originalText = buttonText.textContent;
  buttonText.textContent = '복사되었습니다!';

  setTimeout(() => {
    buttonText.textContent = originalText;
  }, 2000);
}
</script>
