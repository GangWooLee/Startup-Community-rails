<% content_for(:title) { "재인증 필요" } %>

<div class="flex items-center justify-center min-h-[calc(100vh-200px)]">
  <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-8 max-w-md w-full">
    <!-- Header -->
    <div class="text-center mb-6">
      <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-amber-100 mb-4">
        <svg class="w-8 h-8 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
        </svg>
      </div>
      <h1 class="text-xl font-bold text-gray-900">재인증이 필요합니다</h1>
      <p class="text-gray-500 mt-2">
        민감한 작업을 수행하기 전에 비밀번호를 확인해주세요.
      </p>
    </div>

    <!-- Intended Action Info -->
    <% if @intended_action.present? %>
      <div class="bg-gray-50 rounded-lg p-4 mb-6">
        <p class="text-sm text-gray-600">
          <span class="font-medium">요청된 작업:</span>
          <span class="text-gray-900"><%= @intended_action %></span>
        </p>
      </div>
    <% end %>

    <!-- Password Form -->
    <%= form_with url: admin_sudo_path, method: :post, class: "space-y-4" do |f| %>
      <div>
        <label for="password" class="block text-sm font-medium text-gray-700 mb-1">
          비밀번호
        </label>
        <input
          type="password"
          name="password"
          id="password"
          autocomplete="current-password"
          autofocus
          required
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 placeholder-gray-400 transition-colors"
          placeholder="비밀번호를 입력하세요"
        >
      </div>

      <div class="flex gap-3">
        <button
          type="submit"
          class="flex-1 px-4 py-3 bg-blue-500 hover:bg-blue-600 text-white font-medium rounded-lg transition-colors focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
        >
          확인
        </button>

        <% if @return_to.present? %>
          <%= link_to "취소",
            admin_dashboard_path,
            class: "px-4 py-3 bg-gray-100 hover:bg-gray-200 text-gray-700 font-medium rounded-lg transition-colors text-center" %>
        <% end %>
      </div>
    <% end %>

    <!-- Security Notice -->
    <div class="mt-6 pt-6 border-t border-gray-200">
      <p class="text-xs text-gray-500 text-center">
        재인증 후 15분간 민감한 작업이 허용됩니다.
        <br>
        이 요청은 보안 로그에 기록됩니다.
      </p>
    </div>
  </div>
</div>
